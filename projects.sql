CREATE DATABASE projects;
USE projects;
INSERT INTO oracle(
FIRST_NAME,
LAST_NAME,
EMAIL,
MOBILE_NO,
ORGANIZATION_NAME,
ORGANIZATION_ADDRESS,
USER_PASSWORD
) VALUES();
SELECT * FROM oracle;

 SET SQL_SAFE_UPDATES = 0;
 
ALTER TABLE oracle DROP COLUMN DEPARTMENT;



